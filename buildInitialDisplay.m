function initialDisplay = buildInitialDisplay(r, c)
% Initiate a display matrix with all zeroes. 0 means unrevealed, 1 means
% revealed
% Input: r = column number, c = row number
% Output: initial display
% Format of call: buildInitialDisplay(r, c)
initialDisplay = zeros(r, c);
end